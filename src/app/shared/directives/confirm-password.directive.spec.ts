import { ConfirmValidator } from './confirm-password.directive';

describe('ConfirmPassword', () => {
  it('should create an instance', () => {
    // const directive = new ConfirmPasswordValidator();
    // expect(directive).toBeTruthy();
  });
});
